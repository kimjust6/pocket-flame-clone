<script server>
    // Use server-side helper to fetch the Discord Bot token (kept out of source)
    const { sendDiscordMessage2 } = resolve('../../utils/common')
    const result = sendDiscordMessage2(
        'Hello from PocketBase Hooks!',
        '90909125164163072'
    )
</script>
<%= JSON.stringify(result) %>
